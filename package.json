{
  "name": "liquid-intelligence",
  "version": "1.0.0",
  "description": "Premium interactive presentation of advanced 3D web effects — one chrome object, ten effects, one continuous scroll.",
  "scripts": {
    "build": "esbuild src/main.js --bundle --minify --format=iife --outfile=dist/main.js",
    "dev": "esbuild src/main.js --bundle --format=iife --outfile=dist/main.js --watch --servedir=.",
    "serve": "esbuild src/main.js --bundle --format=iife --outfile=dist/main.js --servedir=."
  },
  "repository": {
    "type": "git",
    "url": "http://local_proxy@127.0.0.1:41729/git/Romyleads/test1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "gsap": "^3.15.0",
    "lenis": "^1.3.25",
    "three": "^0.185.1"
  },
  "devDependencies": {
    "esbuild": "^0.28.1"
  },
  "private": true
}
